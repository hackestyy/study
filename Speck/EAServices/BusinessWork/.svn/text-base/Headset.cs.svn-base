using System.ServiceModel;
using ZteApp.ProductService.EAServicesProvision;

namespace ZteApp.ProductService.EAServices.BusinessWork
{
    public partial class Service : IHeadset
    {
        public bool IsHeadsetIn()
        {
            return (new Headset().IsHeadsetIn());
        }
    }
}
